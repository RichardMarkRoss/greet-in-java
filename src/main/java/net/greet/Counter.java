package net.greet;
import net.greet.*;
public class Counter {
    private Greet greet;
    private int counter;
    public Counter(String counter) {
        if(greet == greet){
            this.counter++;
        }
    }
}
